[package]
name = "netlib"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.11.1"
chrono = "0.4.43"
itoa = "1.0.17"
mio = { version = "1.1.1", features = ["net", "os-poll"] }
ringbuf = "0.4.8"
zerocopy = { version = "0.8.39", features = ["derive"] }